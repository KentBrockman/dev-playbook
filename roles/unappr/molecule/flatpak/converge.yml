---
- name: Converge
  hosts: all
  tasks:
    - name: "Include unappr"
      include_role:
        name: "unappr"
      vars:
        applications:
          - name: test_package
            provider: flatpak
